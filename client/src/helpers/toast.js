import { toast } from "react-toastify";

export const toastConnected = () => toast("Connected to MetaMask");
export const toastInstallMetamask = () => toast("🦊 You must install metamask!");
export const toastPleaseAddWebsite = () => toast("🥲 Please add website to metamask");
